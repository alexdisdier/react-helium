export default theme => ({
  root: {
    height: '100%',
    border: `1px solid ${theme.black}`
  },
  controlsWrapper: {
    display: 'flex',
    justifyContent: 'center'
  },
  controlsButtonWrapper: {
    margin: 7
  }
});
