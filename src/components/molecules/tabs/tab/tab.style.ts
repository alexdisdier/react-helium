export default theme => ({
  root: {
    display: 'inline-block',
    marginRight: 20,
    paddingBottom: 10,
    marginBottom: -2,
    fontSize: 12,
    color: theme.grey1,
    '&:hover': {
      cursor: 'pointer'
    }
  }
});
